[General]
name = KDE
tags = v4.98.0;v4.99.0
branches = master;frameworks
tarballs = 4.97.0;4.98.0;4.99.0
defaulttarget = master
tarballUrl = http://download.kde.org/unstable/frameworks/${VERSION}/${PACKAGE_NAME}-${VERSION}.tar.xz
tarballDigestUrl = http://download.kde.org/unstable/frameworks/${VERSION}/${PACKAGE_NAME}-${VERSION}.tar.xz.sha1
tarballInstallSrc = ${PACKAGE_NAME}-${VERSION}
gitUrl = [git]kde:${PACKAGE_NAME}