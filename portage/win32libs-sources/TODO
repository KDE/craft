- expat-src msys --> fixed cmake build system
- gettext-src binary
- giflib-src binary --> fixed cmake build system
- iconv-src --> fixed cmake build system for win_iconv-src
- jpeg-src binary, replace with bic-version jpeg7
- libgmp-src msys
- libgpod-src msys
- libnova-src msys
- libxml2-src binary/msys --> fixed cmake build system
- libxslt-src binary --> Patrick
- openssl-src platform specific
- tiff-src binary
- sqlite-src
- libcurl-src add cmake install targets to cmake
- shared-mime-info-src update buldscript fix x64 compilation
- lcms-src msys

mhhh
- boost headers

special libs which need both compilers
- dbus

don't know how to compile this
- subversion
