diff U3w dbus/dbus-sysdeps-win.c.orig dbus/dbus-sysdeps-win.c
--- dbus/dbus-sysdeps-win.c.orig	Mon Oct 04 13:02:55 2010
+++ dbus/dbus-sysdeps-win.c	Mon Oct 04 13:21:00 2010
@@ -1372,6 +1372,8 @@
         }
     }
 
+  _dbus_fd_set_close_on_exec (fd);
+
   if (!_dbus_set_fd_nonblocking (fd, error) )
     {
       closesocket (fd);
@@ -1561,6 +1563,7 @@
 
   for (i = 0 ; i < nlisten_fd ; i++)
     {
+      _dbus_fd_set_close_on_exec (listen_fd[i]);
       if (!_dbus_set_fd_nonblocking (listen_fd[i], error))
         {
           goto failed;
