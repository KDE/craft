diff -Nru -x '*~' kdelibs-4.10.0.orig/kdeui/windowmanagement/kwindowsystem_win.cpp kdelibs-4.10.0/kdeui/windowmanagement/kwindowsystem_win.cpp
--- kdelibs-4.10.0.orig/kdeui/windowmanagement/kwindowsystem_win.cpp	2013-01-23 22:44:20.000000000 +0100
+++ kdelibs-4.10.0/kdeui/windowmanagement/kwindowsystem_win.cpp	2013-02-19 12:36:12.374805300 +0100
@@ -126,6 +126,7 @@
 
 void KWindowSystemPrivate::activate ( )
 {
+#if 0
 	//prevent us from doing the same over and over again
 	if(activated)
 		return;
@@ -150,6 +151,7 @@
 
     //fetch window infos
     reloadStackList();
+#endif
 }
 
 KWindowSystemPrivate::~KWindowSystemPrivate()
@@ -317,7 +319,7 @@
 {
     KWindowSystem::s_d_func()->stackingOrder.clear();
     KWindowSystem::s_d_func()->winInfos.clear();
-    EnumWindows((WNDENUMPROC)EnumWindProc, 0 );
+//    EnumWindows((WNDENUMPROC)EnumWindProc, 0 );
 }
 
 
diff -Nru -x '*~' kdelibs-4.10.0.orig/kjs/CMakeLists.txt kdelibs-4.10.0/kjs/CMakeLists.txt
--- kdelibs-4.10.0.orig/kjs/CMakeLists.txt	2013-01-23 22:44:27.000000000 +0100
+++ kdelibs-4.10.0/kjs/CMakeLists.txt	2013-02-19 12:34:46.153745100 +0100
@@ -234,7 +234,7 @@
 # 'kjs_bin' because cmake doesn't like having a lib and app with the same name
 kde4_add_executable(kjs_bin NOGUI ${kjs_SRCS})
 
-set_target_properties(kjs_bin PROPERTIES OUTPUT_NAME ${kjs_bin_OUTPUT_NAME})
+set_target_properties(kjs_bin PROPERTIES RUNTIME_OUTPUT_NAME ${kjs_bin_OUTPUT_NAME})
 
 target_link_libraries(kjs_bin ${KJSLIBNAME})
 
diff -Nru -x '*~' kdelibs-4.10.1.orig/kdecore/io/kurl.cpp kdelibs-4.10.1/kdecore/io/kurl.cpp
--- kdelibs-4.10.1.orig/kdecore/io/kurl.cpp	2013-03-01 07:26:13.643785000 +0100
+++ kdelibs-4.10.1/kdecore/io/kurl.cpp	2013-03-04 14:52:45.501009000 +0100
@@ -1686,7 +1686,7 @@
 static QString _relativePath(const QString &base_dir, const QString &path, bool &isParent)
 {
    QString _base_dir(QDir::cleanPath(base_dir));
-   QString _path(QDir::cleanPath(path.isEmpty() || (path[0] != QLatin1Char('/')) ? _base_dir+QLatin1Char('/')+path : path));
+   QString _path(QDir::cleanPath(path.isEmpty() || QDir::isRelativePath(path) ? _base_dir+QLatin1Char('/')+path : path));
 
    if (_base_dir.isEmpty())
       return _path;
diff -Nru -x '*~' kdelibs-4.10.1.orig/kdeui/actions/krecentfilesaction.cpp kdelibs-4.10.1/kdeui/actions/krecentfilesaction.cpp
--- kdelibs-4.10.1.orig/kdeui/actions/krecentfilesaction.cpp	2013-03-01 07:26:13.759783000 +0100
+++ kdelibs-4.10.1/kdeui/actions/krecentfilesaction.cpp	2013-03-05 01:27:02.932139300 +0100
@@ -169,7 +169,12 @@
     // remove file if already in list
     foreach (QAction* action, selectableActionGroup()->actions())
     {
+#ifdef Q_OS_WIN
+      const QString tmpFile = url.isLocalFile() ? QDir::toNativeSeparators( d->m_urls[action].pathOrUrl() ) : d->m_urls[action].pathOrUrl();
+      if ( tmpFile.endsWith(file) )
+#else
       if ( d->m_urls[action].pathOrUrl().endsWith(file) )
+#endif
       {
         removeAction(action)->deleteLater();
         break;
