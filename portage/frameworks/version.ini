[General]
name = KF5
tags = v4.98.0;v4.99.0
branches = master;frameworks
tarballs = 5.0.0;5.1.0;5.2.0
defaulttarget = master
tarballUrl = http://download.kde.org/stable/frameworks/${VERSION}/${PACKAGE_NAME}-${VERSION}.tar.xz
tarballDigestUrl = http://download.kde.org/stable/frameworks/${VERSION}/${PACKAGE_NAME}-${VERSION}.tar.xz.sha1
tarballInstallSrc = ${PACKAGE_NAME}-${VERSION}
gitUrl = [git]kde:${PACKAGE_NAME}
